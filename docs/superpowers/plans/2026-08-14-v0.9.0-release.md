# v0.9.0 Release Plan — Subscription CLIs + named ask

**Date:** 2026-08-14
**Branch:** develop (ahead of origin by 14 commits + uncommitted 0.9.0 work)
**Master:** still v0.7.0 — the v0.8.0 bump on develop was never tagged/released

## Goal

Ship one git-flow release that lets a host ride monthly subscriptions (Claude Code, SuperGrok, Antigravity/`agy`, Codex) **and** pin them for parallel reviews, without accidentally spending Max/Pro quota on `get_fastest`.

## In scope for v0.9.0 (this release)

Already implemented in the working tree (uncommitted):

1. CLI counts as configured with no API key
2. Headless spawn (`-p`), temp cwd, plan/no-tools
3. Grok models `grok-4.6` / `grok-4.5`
4. Claude Code (`opus`/`sonnet`/`haiku`) and Codex if on PATH
5. Gemini subscription via **`agy`** (old `gemini` CLI deprecated June 2026); provider key stays `gemini`
6. `ask(model_ids=…, providers=…)`; auto-pick excludes CLI
7. Setup guides for grok / gemini(agy) / claude / codex / xai

Finish before tag:

- Docs no longer say a hardcoded "21 providers"
- Architecture + MCP instructions mention subscription CLIs
- Dashboard copy mentions `agy` if it still says only grok/gemini
- Tests green (`pytest tests/ -k "not startup_refresh"` plus isolated server list tests)
- Single commit on develop for the 0.9.0 slice
- Push develop
- Git-flow: merge develop → master `--no-ff`, tag `v0.9.0`, push `master develop --tags`

## Out of scope (v0.10.0+)

Parked from the OpenCode/Aider review — do **not** block this release:

| Item | Why later |
|------|-----------|
| Models.dev as catalog source | New fetch path + mapping; needs its own tests |
| GitHub Copilot subscription provider | Device-login OAuth, not a local CLI we already have |
| Named roles (strong/fast/weak) | API surface change; `ask(providers=)` is enough for now |
| Anthropic/OpenAI/DeepSeek HTTPS first-party keys | Separate from subscription riders |

## Review (self-check)

- **Scope:** One product story (ride subscriptions + pin them). Does not mix in a new catalog backend.
- **Risk:** `create_server()` startup refresh can hang tests (pre-existing v0.8.0). Do not block the release on rewriting that hook; exclude `test_startup_refresh_runs_and_logs` if it still hangs.
- **Version:** 0.9.0 is already in `pyproject.toml` and `__init__.py`. Master never got 0.8.0 — this tag is the first public CLI-subscription release.
- **Git flow:** Work is already on `develop` (not a feature branch). Commit there, then release merge. No amend.
- **Authorship:** Commit messages must not include Co-Authored-By.

## Acceptance

- [x] `gemini` provider registered with `cmd=agy` when `agy` is on PATH
- [x] `claude` / `grok` registered when binaries exist
- [x] `ask(..., providers=["claude","grok","gemini"])` exists on the MCP tool
- [x] Default `get_fastest` / `get_workers` / `ask` do not auto-pick CLI
- [x] Docs + README mention Antigravity / subscriptions
- [x] pytest 221 passed
- [x] `v0.9.0` tag on master, `origin/master` and `origin/develop` updated
